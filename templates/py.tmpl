# -*- coding: utf-8 -*-
import math
import string
import itertools
import fractions
import heapq
import collections
import re
import array
import bisect

class ${ClassName}:
    def ${Method.Name}(self, ${Method.Params}):
        return ${Method.ReturnType;zeroval}

${CutBegin}
${<TestCode}
${CutEnd}
